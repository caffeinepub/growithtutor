{
  "kind": "build_request",
  "title": "Set public domain to growithtutor.com",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Configure the React app to use the public domain \"growithtutor.com\" for public-domain-based URL generation (PUBLIC_DOMAIN / getPublicUrl / getPublicAssetUrl), ensuring production builds resolve to growithtutor.com rather than a different environment-provided value.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-8"
        ],
        "quotes": [
          "change the domain to \"growithtutor.com\""
        ]
      },
      "acceptanceCriteria": [
        "When the app is built for production with the project’s default configuration, `PUBLIC_DOMAIN` evaluates to `growithtutor.com`.",
        "Any code paths that generate public URLs via `getPublicUrl(...)` / `getPublicAssetUrl(...)` produce `https://growithtutor.com/...` URLs.",
        "No changes are made to read-only paths listed in `frontend.immutablePaths`."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Ensure the standalone landing page domain configuration uses \"growithtutor.com\" consistently (the standalone public domain constant must be set to growithtutor.com).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-8"
        ],
        "quotes": [
          "change the domain to \"growithtutor.com\""
        ]
      },
      "acceptanceCriteria": [
        "`frontend/static/growwithtutor-standalone/index.html` has `STANDALONE_PUBLIC_DOMAIN` set to `growithtutor.com`.",
        "Standalone landing page asset URLs constructed by the helper function resolve to `https://growithtutor.com/...`."
      ]
    }
  ],
  "constraints": [
    "Frontend immutable paths must not be edited: frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui."
  ],
  "nonGoals": [
    "Configuring external DNS records for growithtutor.com.",
    "Internet Computer custom domain/certificate setup steps outside the codebase."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}